{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "NicePod MCP Configuration Schema",
  "description": "Schema for validating Model Context Protocol server configurations within the Madrid Resonance Protocol.",
  "type": "object",
  "required": [
    "mcpServersCollection"
  ],
  "properties": {
    "mcpServersCollection": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": [
          "name",
          "type",
          "config"
        ],
        "properties": {
          "name": {
            "type": "string",
            "pattern": "^[a-z0-9-]+-mcp-server$"
          },
          "type": {
            "type": "string",
            "enum": [
              "builtin",
              "external"
            ]
          },
          "status": {
            "type": "string",
            "enum": [
              "active",
              "maintenance",
              "disabled"
            ],
            "default": "active"
          },
          "config": {
            "type": "object",
            "additionalProperties": true
          }
        }
      }
    }
  }
}