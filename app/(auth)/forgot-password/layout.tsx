/**
 * ARCHIVE: app/(auth)/forgot-password/layout.tsx
 * VERSION: 1.0 (NicePod Recovery Metadata - Industrial SEO Standard)
 * PROTOCOLO: MADRID RESONANCE V4.0
 * MISSION: Define identity and indexing rules for the access recovery tunnel.
 * INTEGRITY LEVEL: 100% (Sovereign / Zero Abbreviations / Production-Ready)
 */

import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Recuperar Acceso | NicePod Intelligence",
  description: "Portal de recuperación de identidad y sincronización de llave de acceso.",
};

export default function ForgotPasswordLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
